language: dart
dart:
# Install the latest stable release
- stable
dart_task:
- test: --exclude-tags no-xvfb
  xvfb: false
script:
- pub run test/xml2json_test.dart
- global activate webdev
- webdev build --no-release --output test:build
- pub run build_runner test --fail-on-severe -- -p chrome